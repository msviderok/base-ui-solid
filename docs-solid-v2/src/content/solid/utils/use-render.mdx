---
title: useRender
order: 2
description: A hook for rendering Base UI components with custom elements.
---

# useRender

<Subtitle>A hook for rendering Base UI components with custom elements.</Subtitle>

`useRender` is a low-level hook that Base UI uses internally to render its components. It handles the `render` prop pattern, ref forwarding, and prop merging.

## API reference

<Reference name="RenderElement" />
