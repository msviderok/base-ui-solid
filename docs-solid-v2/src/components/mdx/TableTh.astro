---
const { scope, ...props } = Astro.props;
---
{scope === 'row' ? (
  <th scope="row" class="TableCell" {...props}><span class="TableCellInner"><slot /></span></th>
) : (
  <th scope="col" class="TableColumnHeader" {...props}>
    <span class="TableCellInner"><slot /></span>
  </th>
)}
